apiVersion: v1
kind: Service                                                             
metadata:                                                                 
  name: n1an-argo
  labels:
    app: n1an-argo
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5000
    protocol: TCP
  selector:
    app: n1an-argo
